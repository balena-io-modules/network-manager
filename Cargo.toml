[package]
name = "network_manager"
version = "0.1.0"
authors = ["Joseph Roberts <joe@resin.io>"]

[dependencies]
clippy = {version = "*", optional = true}
dbus = "0.4.1"
enum_primitive = "0.1.0"

[features]
default = []
